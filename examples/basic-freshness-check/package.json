{
  "name": "freshguard-basic-example",
  "version": "1.0.0",
  "description": "Basic FreshGuard Core freshness monitoring example",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "tsx setup.ts",
    "monitor": "tsx monitor.ts",
    "start": "pnpm setup && pnpm monitor",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "@thias-se/freshguard-core": "^0.5.2",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.30",
    "tsx": "^4.21.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
